# `npm run build:single` makes the embedded preview. Its Pixel Postcard codes open the viewer on the
# GitHub Pages site that .github/workflows/deploy.yml publishes; change this when hosting elsewhere.
VITE_PUBLIC_URL=https://pukkingdragon123.github.io/glowing-octo-chainsaw/
