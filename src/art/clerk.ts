import * as THREE from 'three';

/** Placeholder for the axolotl-in-a-shark-mech clerk (replaced by the real rig). */
export class Clerk {
  readonly root = new THREE.Group();
  update(_dt: number, _t: number) {}
}
